Code implemented in unscramble.c

Code reads in decryption commands from a specified file (sample: alice.script)
Commands are formatted as: <operation> <size> <offset> <constant>
All operations are performed using unsigned arithmetic, ignoring overflow.

Implements decryption commands on scrambled file to get output file
